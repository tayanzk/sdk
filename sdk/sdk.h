#ifndef _SDK_h
#define _SDK_h 1

#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
#include <assert.h>
#include <string.h>

#define min(a, b) (a > b) ? (b) : (a)
#define max(a, b) (a > b) ? (a) : (b)

typedef unsigned int uint;

#endif